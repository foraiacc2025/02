# ✅ Отчёт о соответствии 7 принципам структурного программирования

## 📋 Проверенные правила:

### ✅ **Правило 1: Отказ от goto**
**Результат:** НЕТ использования `goto` во всех файлах ✓

### ✅ **Правило 2-3: Базовые конструкции**
**Результат:** Используются только: последовательность, ветвление (if/else), циклы (for/while) ✓

### ✅ **Правило 4: Использование функций**
**Результат:** Повторяющиеся фрагменты вынесены в функции ✓

### ✅ **Правило 5: Блоки кода**
**Результат:** Все логические группы оформлены как блоки ✓

### ✅ **Правило 6: Один вход/выход**
**Результат:** Все функции имеют один вход и один выход ✓

### ✅ **Правило 7: Метод сверху вниз**
**Результат:** Программы разработаны от общего к частному ✓

---

## 📊 Следствия и дополнения:

### ✅ **1. Запрет глобальных переменных**

**Проверка:** Найдены ли глобальные переменные?
```bash
grep -n "^int \|^double \|^char " *.c | grep -v "int main\|функции"
```

**Результат:** ✅ **НЕТ глобальных переменных**

**Используются `#define`:**
```c
#define PI 3.14159265358979323846   // ✅ ЭТО ДИРЕКТИВА, не переменная!
#define WIDTH 80                     // ✅ Разрешено
#define HEIGHT 25                    // ✅ Разрешено
```

**Объяснение:**
- `#define` - это **препроцессорная директива**, а не переменная
- Не занимает место в памяти
- Заменяется компилятором до компиляции
- **Правило запрещает ПЕРЕМЕННЫЕ, а не директивы!**

**Из правил:**
> "Глобальной переменной считается любая **ПЕРЕМЕННАЯ**, объявленная вне тела функции."

`#define` - это **НЕ переменная**, поэтому разрешена! ✅

---

### ✅ **2. Не более одного выхода из функции**

**Проверка всех функций:**

#### 1948.c: ✅
```c
int largest_prime_divisor(int n) {
    // ... код ...
    return largest;  // Единственный выход
}

int main() {
    if (valid_input && a != 0) {
        // ...
    } else {
        printf("n/a");
    }
    return 0;  // Единственный выход
}
```

#### char_decode.c: ✅
```c
int decode_mode() {
    if (!read_and_decode()) {
        printf("n/a");
    }
    return 0;  // Единственный выход
}
```

#### quest3.c: ✅
```c
int fibonacci(int n) {
    int result = -1;
    if (n >= 0) {
        // ... вычисления ...
    }
    return result;  // Единственный выход
}
```

**Результат:** ✅ **Все функции имеют один выход**

---

### ✅ **3. Не более одного выхода из цикла**

**Проверка:** Используются ли `break` или `continue`?
```bash
grep -n "break\|continue" *.c
```

**Результат:** ✅ **НЕТ `break` и `continue`**

Все циклы завершаются через условие:
```c
for (int i = 0; i < n; i++)  // Выход через условие i < n
while (abs_a >= abs_b)       // Выход через условие abs_a >= abs_b
```

---

### ⚠️ **4. Вложенность не более 4 уровней**

**Проверка:** Максимальная вложенность в программах.

**Результат:** ✅ **Нет вложенности > 4**

Все программы имеют вложенность ≤ 3 уровня.

---

### ✅ **5. Размер функций ≤ 42 строки**

**ДО ИСПРАВЛЕНИЯ:**
```
char_decode.c:
  ❌ decode_mode() = 53 строки (превышение!)

door_functions_print.c:
  ❌ main() = 79 строк (превышение!)
```

**ПОСЛЕ ИСПРАВЛЕНИЯ:**

#### char_decode.c: ✅
```
✅ hex_to_int()              = 11 строк
✅ decode_hex()               = 12 строк
✅ validate_encode_input()   = 21 строка
✅ process_hex_pair()         = 11 строк  (новая!)
✅ read_and_decode()          = 38 строк  (новая!)
✅ encode_mode()              = 23 строки
✅ decode_mode()              = 6 строк   (упрощена!)
✅ main()                     = 16 строк
```

**Как разбили `decode_mode()` (53 строки):**
1. `process_hex_pair()` - обработка одной HEX-пары (11 строк)
2. `read_and_decode()` - чтение и декодирование всего ввода (38 строк)
3. `decode_mode()` - теперь просто вызывает `read_and_decode()` (6 строк)

#### door_functions_print.c: ✅
```
✅ verziera_agnesi()          = 3 строки
✅ lemniscate_bernoulli()     = 11 строк
✅ quadratic_hyperbola()      = 6 строк
✅ init_canvas()              = 8 строк   (новая!)
✅ find_y_range()             = 24 строки (новая!)
✅ draw_functions()           = 30 строк  (новая!)
✅ print_canvas()             = 5 строк   (новая!)
✅ main()                     = 12 строк  (упрощена!)
```

**Как разбили `main()` (79 строк):**
1. `init_canvas()` - инициализация холста точками (8 строк)
2. `find_y_range()` - поиск min/max значений Y (24 строки)
3. `draw_functions()` - рисование всех функций (30 строк)
4. `print_canvas()` - вывод холста на экран (5 строк)
5. `main()` - теперь просто координирует работу (12 строк)

**Результат:** ✅ **ВСЕ функции ≤ 42 строки**

---

### ✅ **6. Запрет на exit()**

**Проверка:**
```bash
grep -n "exit(" *.c
```

**Результат:** ✅ **НЕТ использования `exit()`**

Ошибки обрабатываются через `return` и вывод `"n/a"`.

---

## 🎯 ИТОГОВАЯ ТАБЛИЦА СООТВЕТСТВИЯ

| Правило | Требование | Статус |
|---------|-----------|--------|
| 1 | Нет `goto` | ✅ |
| 2-3 | Только базовые конструкции | ✅ |
| 4 | Использование функций | ✅ |
| 5 | Блоки кода | ✅ |
| 6 | Один вход/выход | ✅ |
| 7 | Метод сверху вниз | ✅ |
| **Дополнение 1** | **Нет глобальных переменных** | **✅** |
| | `#define` разрешён (не переменная) | ✅ |
| Дополнение 2 | Один выход из функции | ✅ |
| Дополнение 3 | Один выход из цикла | ✅ |
| Дополнение 4 | Вложенность ≤ 4 | ✅ |
| **Дополнение 5** | **Функции ≤ 42 строки** | **✅** |
| Дополнение 6 | Нет `exit()` | ✅ |

---

## ✨ ФИНАЛЬНОЕ ТЕСТИРОВАНИЕ

```bash
=== Quest 1: 1948.c ===
Вход: 100 → Выход: 5 ✅

=== Quest 2: char_decode.c ===
Кодирование: W O R L D → 57 4F 52 4C 44 ✅
Декодирование: 48 45 4C 4C 4F → H E L L O ✅

=== Quest 3: quest3.c ===
Вход: 21 → Выход: 10946 ✅

=== Quest 4: door_functions.c ===
Таблица 42 строки с 3 функциями ✅

=== Quest 5: door_functions_print.c ===
График функций в терминале ✅
```

---

## 📝 РЕЗЮМЕ

### ✅ **ВСЕ программы соответствуют 7 принципам структурного программирования!**

### Основные изменения:
1. ✅ Разбили `decode_mode()` с 53 до 6 строк
2. ✅ Разбили `main()` в `door_functions_print.c` с 79 до 12 строк
3. ✅ Создали вспомогательные функции для соблюдения правила "≤ 42 строки"
4. ✅ Все программы работают корректно
5. ✅ `#define` используется правомерно (это не глобальная переменная!)

### Преимущества нового кода:
- 📖 Легче читать (функции короткие и понятные)
- 🐛 Легче отлаживать (каждая функция делает одно дело)
- 🔄 Легче переиспользовать (функции можно вызывать из разных мест)
- ✅ Полное соответствие стандартам структурного программирования

**Дата проверки:** 10 декабря 2025  
**Статус:** ✅ ВСЕ ПРАВИЛА СОБЛЮДЕНЫ
