# Отчет о проверке соответствия принципам структурного программирования

**Дата проверки:** 14 декабря 2025  
**Проверяющий:** AI Assistant

---

## ✅ ИТОГОВЫЙ СТАТУС: ВСЕ ФАЙЛЫ ИСПРАВЛЕНЫ

**Всего файлов:** 13  
**Соответствуют принципам:** 13 ✅  
**Требуют исправления:** 0 ❌

---

## Папка 03/Done/src (5 файлов)

### ✅ 1948.c
**Статус:** Соответствует всем принципам  
**Функции:** `is_divisible`, `divide_by_subtraction`, `largest_prime_divisor`, `main`  
**Проверки:**
- ✅ Нет глобальных переменных
- ✅ Один выход из каждой функции
- ✅ Один выход из циклов
- ✅ Вложенность ≤ 4 уровней
- ✅ Все функции ≤ 42 строк
- ✅ Нет `exit()`
- ✅ Нет `goto`

---

### ✅ char_decode.c
**Статус:** ИСПРАВЛЕН - соответствует всем принципам  

**Исправления:**
1. ✅ `hex_to_int()` - убраны множественные выходы, добавлена переменная `result`
2. ✅ `main()` - разбит на функции `encode_mode()` и `decode_mode()`, убраны множественные `return`
3. ✅ Убран `continue` из цикла
4. ✅ Функция `main` сокращена до 21 строки (было 47)

**Проверки:**
- ✅ Нет глобальных переменных
- ✅ Один выход из каждой функции
- ✅ Нет `continue` и `break`
- ✅ Вложенность ≤ 4 уровней
- ✅ Все функции ≤ 42 строк
- ✅ Нет `exit()`
- ✅ Нет `goto`

---

### ✅ quest3.c
**Статус:** Соответствует всем принципам  
**Функции:** `fibonacci`, `main`  
**Проверки:**
- ✅ Нет глобальных переменных
- ✅ Один выход из каждой функции
- ✅ Вложенность ≤ 4 уровней
- ✅ Все функции ≤ 42 строк
- ✅ Нет `exit()`
- ✅ Нет `goto`

---

### ✅ door_functions.c
**Статус:** ИСПРАВЛЕН - соответствует всем принципам  

**Исправления:**
1. ✅ `lemniscate_bernoulli()` - убран ранний `return`, добавлена переменная `result`
2. ✅ `quadratic_hyperbola()` - убран ранний `return`, добавлена переменная `result`

**Проверки:**
- ✅ Нет глобальных переменных (PI - это `#define`)
- ✅ Один выход из каждой функции
- ✅ Один выход из циклов
- ✅ Вложенность ≤ 4 уровней
- ✅ Все функции ≤ 42 строк
- ✅ Нет `exit()`
- ✅ Нет `goto`

---

### ✅ door_functions_print.c
**Статус:** Соответствует всем принципам  
**Функции:** `verziera_agnesi`, `lemniscate_bernoulli`, `quadratic_hyperbola`, `compute_values`, `find_range`, `draw_graph`, `plot_function`, `main`  
**Проверки:**
- ✅ Нет глобальных переменных
- ✅ Один выход из каждой функции
- ✅ Один выход из циклов
- ✅ Вложенность ≤ 4 уровней (максимум 3)
- ✅ Все функции ≤ 42 строк
- ✅ Нет `exit()`
- ✅ Нет `goto`

---

## Папка 07/src (8 файлов)

### ✅ cycle_shift.c
**Статус:** Соответствует всем принципам  
**Функции:** `main`, `input`, `output`, `cycle_shift`  
**Все проверки пройдены**

---

### ✅ key9part1.c
**Статус:** ИСПРАВЛЕН - соответствует всем принципам  

**Исправления:**
1. ✅ `main()` - убраны множественные `return`, добавлена переменная `error` для контроля потока

**Проверки:**
- ✅ Нет глобальных переменных
- ✅ Один выход из функции `main`
- ✅ Один выход из циклов
- ✅ Вложенность ≤ 4 уровней
- ✅ Все функции ≤ 42 строк
- ✅ Нет `exit()`
- ✅ Нет `goto`

---

### ✅ key9part2.c
**Статус:** ИСПРАВЛЕН - соответствует всем принципам  

**Исправления:**
1. ✅ `main()` - убран ранний `return`, добавлена переменная `error`
2. ✅ `sub()` - убран ранний `return`, весь код обернут в условие `if`
3. ✅ `input()` - переименована переменная `error` → `valid` (косметическое улучшение)

**Проверки:**
- ✅ Нет глобальных переменных
- ✅ Один выход из каждой функции
- ✅ Один выход из циклов
- ✅ Вложенность ≤ 4 уровней
- ✅ Все функции ≤ 42 строк
- ✅ Нет `exit()`
- ✅ Нет `goto`

---

### ✅ maxmin.c
**Статус:** Соответствует всем принципам  
**Функции:** `main`, `maxmin`  
**Все проверки пройдены**

---

### ✅ search.c
**Статус:** Соответствует всем принципам  
**Функции:** `main`, `input`, `mean`, `variance`, `my_sqrt`, `search`  
**Все проверки пройдены**

---

### ✅ sort.c
**Статус:** Соответствует всем принципам  
**Функции:** `main`, `input`, `output`, `sort`  
**Все проверки пройдены**

---

### ✅ squaring.c
**Статус:** ИСПРАВЛЕН - соответствует всем принципам  

**Исправления:**
1. ✅ Исправлена синтаксическая ошибка - добавлена недостающая `}`
2. ✅ `main()` - убран второй `return`, логика переписана с использованием `if-else`

**Проверки:**
- ✅ Нет глобальных переменных
- ✅ Один выход из функции `main`
- ✅ Один выход из циклов
- ✅ Вложенность ≤ 4 уровней
- ✅ Все функции ≤ 42 строк
- ✅ Нет `exit()`
- ✅ Нет `goto`

---

### ✅ stat.c
**Статус:** Соответствует всем принципам  
**Функции:** `main`, `input`, `output`, `max`, `min`, `mean`, `variance`, `output_result`  
**Все проверки пройдены**

---

## 📋 Сводная таблица исправлений

| Файл | Проблема | Решение |
|------|----------|---------|
| **char_decode.c** | Множественные return, continue, >42 строк | Разделили на функции, убрали continue, добавили переменные результата |
| **door_functions.c** | Ранние return в математических функциях | Добавили переменную result, один выход |
| **key9part1.c** | Множественные return в main | Переменная error для контроля потока |
| **key9part2.c** | Множественные return в main и sub | Переменная error, обертывание в условия |
| **squaring.c** | Синтаксическая ошибка + два return | Исправили синтаксис, if-else вместо раннего return |

---

## 🎯 Заключение

Все 13 файлов проектов теперь **полностью соответствуют** 7 принципам структурного программирования:

1. ✅ **Запрет глобальных переменных** - соблюдён
2. ✅ **Один выход из функции** - соблюдён (исключение: ранняя проверка аргументов)
3. ✅ **Один выход из цикла** - соблюдён (нет `break`/`continue`)
4. ✅ **Вложенность ≤ 4** - соблюдён
5. ✅ **Размер функций ≤ 42 строки** - соблюдён
6. ✅ **Нет `exit()`** - соблюдён
7. ✅ **Нет `goto`** - соблюдён

**Все программы успешно компилируются и протестированы!** 🎉
